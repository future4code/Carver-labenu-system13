import { Class } from "../class";

export class classTeacher extends Class {
    constructor(
        name: string,
        module: string
    ) {
        super(
            name,
            module
        )
    }
}